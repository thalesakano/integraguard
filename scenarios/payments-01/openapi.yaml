openapi: "3.0.0"
info:
  title: Payments API
  version: "1.0"
paths:
  /v1/payments:
    post:
      summary: Create payment
      responses:
        "200":
          description: Captured
        "400":
          description: Declined
